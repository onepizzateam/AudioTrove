Usage: python -m audiotrove.cli.main [OPTIONS] COMMAND [ARGS]...

  AudioTrove: Open-source audio data curation pipeline.

Options:
  --version      Show the version and exit.
  -v, --verbose  Enable verbose (DEBUG) logging.
  --help         Show this message and exit.

Commands:
  curate   Curate audio files from INPUT_PATH into OUTPUT_PATH.
  inspect  Show statistics for an audio directory without filtering.
